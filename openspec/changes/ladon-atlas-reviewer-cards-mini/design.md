# Design Notes

## Card Boundaries

Cards summarize only data already present in the atlas graph. Missing evidence
or non-claim fields are rendered explicitly as `not recorded in atlas` rather
than inferred.

## Stable Shape

Each card includes:

- report path
- root module
- extraction backend
- top findings
- review regions
- strongest evidence
- known non-claims
- source report JSON/text links

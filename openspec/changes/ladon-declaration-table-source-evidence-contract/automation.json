{
  "commands": [
    "openspec validate ladon-declaration-table-source-evidence-contract --strict",
    "uv run pytest -q tests",
    "uv run python scripts/python_quality.py --strict",
    "uv run python -m compileall -q src tests scripts"
  ]
}

# Child 2: `ladon-atlas-sqlite-query-mini`

Export report-atlas data into a deterministic SQLite database and provide
canned queries that answer reviewer questions directly.

Expected queries:

- modules or roots that recur as hotspots across reports
- declarations that recur across reports
- roots with review-region pressure
- reports with proof-family pressure
- OpenSpec packets with metadata/task drift, if packet rows are available

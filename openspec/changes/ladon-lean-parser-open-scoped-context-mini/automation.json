{
  "commands": [
    "openspec validate ladon-lean-parser-open-scoped-context-mini --strict",
    "uv run pytest tests/test_lean_extraction.py -q",
    "uv run pytest -q",
    "uv run python scripts/python_quality.py --strict"
  ]
}

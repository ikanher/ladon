# Tasks

- [x] Reproduce the parser-helper failure on `Mf/DP/OptimalSubsamplingBISLR.lean`.
- [x] Add parser-context transitions for `open scoped` and plain `open` commands.
- [x] Add an imported scoped syntax regression test.
- [x] Validate the failing matrix-factorization owner now extracts with the Lean backend.
- [x] Validate with `uv run pytest tests/test_lean_extraction.py -q`.
- [x] Validate with `uv run pytest -q`.
- [x] Validate with `uv run python scripts/python_quality.py --strict`.

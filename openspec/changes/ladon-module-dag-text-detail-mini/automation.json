{
  "commands": [
    "openspec validate ladon-module-dag-text-detail-mini --strict",
    "uv run pytest -q",
    "uv run python scripts/python_quality.py --strict",
    "uv run python -m compileall -q src tests scripts",
    "uv build"
  ]
}

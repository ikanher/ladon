# Design

This packet is documentation only. It keeps the shared skill aligned with the
standalone project so future Codex instances use the current report sections
instead of falling back to raw unresolved-reference rows.

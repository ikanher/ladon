# Child 4: Wider Root Matrix

Add a maintained local root matrix:

- Quux: `ProofIR`, `Bridge.Example.Core`, selected `Problems/*`, selected
  `Semantics/*`.
- Matrix-factorization: `GaussianCore`, `BSRFactorCore`, `Optimization/*`,
  BIFR/Gamma owners.

Runs should be optional and local, with predicate checks separated from report
generation.

# Child 2: Root Scope Classification

Refine `root_scope_pressure`:

- narrow theorem owner in broad repo;
- intentionally narrow public root;
- likely mistaken root choice;
- true umbrella owner with broad direct import closures.

The finding should keep the component signals but expose a clearer reason.

# Tasks

- [x] Add root-matrix tests for the newly Lean-backed entries.
- [x] Convert selected root-matrix entries to Lean-backed extraction.
- [x] Rerun the default root matrix.
- [x] Run root-matrix and live calibration regressions.
- [x] Run `openspec validate ladon-root-matrix-lean-expansion --strict`.
- [x] Run `uv run pytest -q`.
- [x] Run `uv run python scripts/python_quality.py --strict`.
- [x] Run `uv run python -m compileall -q src tests scripts`.
- [x] Run `uv build`.
